#include <iostream>
using namespace std;

int main() {
    // check if second is square of first
    int a , b;
    cout<<"enter a: ";
    cin>>a;
    cout<<"enter b: ";
    cin>>b;
    if(a*a==b)
    cout<<"second number is a square of first number: ";
    return 0;
}