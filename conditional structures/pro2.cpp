#include <iostream>
using namespace std;

int main() {
    int a , b;
    cout<<"Enter a: ";
    cin>>a;
    cout<<"enter b: ";
    cin>>b;
    if(a==b)
    cout<<"both numbers are equal."<<endl;
    else
    cout<<"both numbers are not equal. ";
    return 0;
}